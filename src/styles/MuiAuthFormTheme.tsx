import { createMuiTheme } from '@material-ui/core';

export const AuthFormDefaultTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#b2f617',
        },
        secondary: {
            main: '#FFFFFF',
        },
        type: 'dark'
    },
    typography: {
        fontFamily: 'mollenregular',
    }
});
