import { createMuiTheme } from '@material-ui/core';

export const ThemePlatform = createMuiTheme({
	palette: {
		primary: {
			main: '#b2f617',
		},
		secondary: {
			main: '#292D30',
		},
		type: 'dark'
	},
});