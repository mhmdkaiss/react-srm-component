import { DisplaySelector } from '@cactus/srm-component';
import React from 'react';

export const DisplaySelectorExample: React.FunctionComponent = () => {
    return (
        <>
            <DisplaySelector onChange={(e) => e}/>
        </>
    );
};
