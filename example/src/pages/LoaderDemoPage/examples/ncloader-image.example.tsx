import { NCLoader } from '@cactus/srm-component';
import React from 'react';

import './ncloader.example.scss';

export const NCLoaderImageExample: React.FunctionComponent = () => {
    return (
        <div className="loader-image">
            <NCLoader />
        </div>
    );
};
