import { NCLoader } from '@cactus/srm-component';
import React from 'react';

import './ncloader.example.scss';

export const NCLoaderExample: React.FunctionComponent = () => {
    return (
        <div className="loader-default">
            <NCLoader />
        </div>
    );
};
